public class Scripture {
    // these are all attributes
    private Reference _reference;
    private string _verseText;

    // constructors - accepts both reference and text, internally stores a list of word objects
    // pass text of scripture as a single string w/ all words in it
    // ---> constructors job = create list, split words in string to create WORD objects for each
    // and put in list
    public Scripture(string inputReference, string inputVerseText)
    {
        _reference = ;
        _verseText = ;
    }
    // methods/behaviors keep track of the reference, the text, and to hide the words
    public HideWords(){}
    public GetRenderedText(){}
    public IsCompletelyHidden(){}
}

// prop tab